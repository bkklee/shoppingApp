name: Submit to TestFlight (iOS)

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows manual trigger from GitHub Actions tab

jobs:
  build-and-submit:
    name: Build & Submit iOS App locally
    runs-on: macos-latest
    steps:
      - name: 🏗 Setup repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: 🏗 Setup Expo and EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install EAS CLI and Dependencies
        run: |
          npm install -g eas-cli
          npm ci

      - name: 🛠 Build Production IPA (Local)
        run: eas build --platform ios --profile production --local --non-interactive --output=./pricebite-hk.ipa
        env:
          EAS_BUILD_NO_EXPO_GO_WARNING: true

      - name: 📤 Submit to App Store Connect
        run: eas submit --platform ios --profile production --path ./pricebite-hk.ipa --non-interactive
        env:
          EXPO_APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.EXPO_APPLE_APP_SPECIFIC_PASSWORD }}
